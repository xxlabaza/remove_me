
server:
  port: 9004

turbine.amqp.port: 9005

security.basic.enabled: false

turbine:
  instanceUrlSuffix: ${management.context-path}/hystrix.stream
  aggregator:
    clusterConfig: ZUUL
  appConfig: zuul


server:
  port: 9004

management:
  port: -1

security.basic.enabled: false

turbine:
  instanceUrlSuffix: ${management.context-path}/hystrix.stream
  aggregator:
    clusterConfig: ZUUL
  appConfig: zuul
